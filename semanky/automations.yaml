---
# automations.yaml

- id: switch_mycka
  alias: "Switch mycka on/off"
  description: "Switch mycka: SOC-based, forced ON at 12:00, SOC check at 20:00"
  trigger:
    - platform: numeric_state
      entity_id: sensor.growatt_battery_battery_soc
      above: 80
      for:
        minutes: 5
      id: switch_mycka_on
    - platform: numeric_state
      entity_id: sensor.growatt_battery_battery_soc
      below: 50
      id: switch_mycka_off
    - platform: time
      at: "12:00:00"
      id: switch_mycka_noon
    - platform: time
      at: "20:00:00"
      id: switch_mycka_evening
  condition:
    - condition: numeric_state
      entity_id: sensor.mycka_power
      below: 10
  action:
    - choose:
      - conditions:
          - condition: trigger
            id: switch_mycka_on
        sequence:
          - service: switch.turn_on
            entity_id: switch.mycka
      - conditions:
          - condition: trigger
            id: switch_mycka_off
          # SOC vypnutí jen mimo poledne-večer
          - condition: time
            after: "20:00:00"
            before: "12:00:00"
        sequence:
          - service: switch.turn_off
            entity_id: switch.mycka
      - conditions:
          - condition: trigger
            id: switch_mycka_noon
        sequence:
          - service: switch.turn_on
            entity_id: switch.mycka
      - conditions:
          - condition: trigger
            id: switch_mycka_evening
          - condition: numeric_state
            entity_id: sensor.growatt_battery_battery_soc
            below: 50
        sequence:
          - service: switch.turn_off
            entity_id: switch.mycka

# =============================================================================
# BOJLER - INSPIRACE (zatím neaktivní, odkomentuj až budeš chtít použít)
# =============================================================================

# Strategie 1: Dump load — bojler jako přebytek když je baterka plná
# Logika: SOC >= 95 % → zapnout bojler, SOC < 85 % → vypnout
# Vhodné pro: maximální využití solaru, baterka má přednost

- id: boiler_dump_load
  alias: "Boiler - dump load when battery full"
  description: "Use boiler as dump load when battery SOC >= 95%"
  trigger:
    - platform: numeric_state
      entity_id: sensor.growatt_battery_battery_soc
      above: 95
      for:
        minutes: 2
      id: boiler_on
    - platform: numeric_state
      entity_id: sensor.growatt_battery_battery_soc
      below: 85
      id: boiler_off
  condition:
    - condition: numeric_state
      entity_id: sensor.siton210_temperature
      below: 70                          # nepřehřívat
  action:
    - choose:
      - conditions:
          - condition: trigger
            id: boiler_on
        sequence:
          - service: switch.turn_on
            entity_id: switch.greenbono_ssr_boiler
      - conditions:
          - condition: trigger
            id: boiler_off
        sequence:
          - service: switch.turn_off
            entity_id: switch.greenbono_ssr_boiler

# Strategie 2: Ranní předehřev — ohřát vodu ráno pokud je dobrá solární předpověď
# Logika: v 7:00 zkontroluj předpověď, pokud > 8 kWh dnes → zapni bojler
# Vhodné pro: zajistit teplou vodu i v případě odpoledního zatažení
#
# - id: boiler_morning_preheat
#   alias: "Boiler - morning preheat if good solar forecast"
#   description: "Preheat boiler in the morning if solar forecast is sufficient"
#   trigger:
#     - platform: time
#       at: "07:00:00"
#   condition:
#     - condition: numeric_state
#       entity_id: sensor.energy_production_today   # Forecast.Solar
#       above: 8                                    # kWh předpověď
#     - condition: numeric_state
#       entity_id: sensor.siton210_temperature
#       below: 45                                   # bojler potřebuje ohřev
#     - condition: numeric_state
#       entity_id: sensor.growatt_battery_battery_soc
#       above: 30                                   # nechceme startovat z prázdna
#   action:
#     - service: switch.turn_on
#       entity_id: switch.greenbono_ssr_boiler
#     - delay: "02:00:00"                           # max 2 hodiny ohřevu
#     - service: switch.turn_off
#       entity_id: switch.greenbono_ssr_boiler

# Strategie 3: Kombinovaná — přebytek výkonu + teplota bojleru
# Logika: PV výkon mínus spotřeba > 1000 W po dobu 10 min → zapnout bojler
# Vhodné pro: přesné sledování přebytku, nezatěžovat baterku
#
# - id: boiler_surplus_power
#   alias: "Boiler - surplus solar power"
#   description: "Turn on boiler when there is surplus PV power"
#   trigger:
#     - platform: template
#       value_template: >
#         {{ (states('sensor.growatt_solar_pv1_power') | float(0)
#           + states('sensor.growatt_solar_pv2_power') | float(0)
#           - states('sensor.growatt_solar_ac_power') | float(0)) > 1000 }}
#       for:
#         minutes: 10
#       id: boiler_on
#     - platform: template
#       value_template: >
#         {{ (states('sensor.growatt_solar_pv1_power') | float(0)
#           + states('sensor.growatt_solar_pv2_power') | float(0)
#           - states('sensor.growatt_solar_ac_power') | float(0)) < 300 }}
#       for:
#         minutes: 5
#       id: boiler_off
#   condition:
#     - condition: numeric_state
#       entity_id: sensor.siton210_temperature
#       below: 65
#   action:
#     - choose:
#       - conditions:
#           - condition: trigger
#             id: boiler_on
#         sequence:
#           - service: switch.turn_on
#             entity_id: switch.greenbono_ssr_boiler
#       - conditions:
#           - condition: trigger
#             id: boiler_off
#         sequence:
#           - service: switch.turn_off
#             entity_id: switch.greenbono_ssr_boiler
  alias: "Switch mycka on/off"
  description: "Switch mycka based on battery SOC"
  trigger:
    - platform: numeric_state
      entity_id: sensor.growatt_battery_battery_soc
      above: 80
      for:
        minutes: 5
      id: switch_mycka_on
    - platform: numeric_state
      entity_id: sensor.growatt_battery_battery_soc
      below: 50
      id: switch_mycka_off
  condition:
    - condition: numeric_state
      entity_id: sensor.mycka_power
      below: 10
  action:
    - choose:
      - conditions:
          - condition: trigger
            id: switch_mycka_on
        sequence:
          - service: switch.turn_on
            entity_id: switch.mycka
      - conditions:
          - condition: trigger
            id: switch_mycka_off
        sequence:
          - service: switch.turn_off
            entity_id: switch.mycka
